server {
    listen 3000;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # API proxying would be handled by API Gateway in production
    # For now, frontend makes direct requests to services
}

